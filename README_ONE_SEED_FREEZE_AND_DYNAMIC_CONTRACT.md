# One-seed evidence freeze and dynamic TWC contract

Run only:

```bash
bash RUN_ONE_SEED_FREEZE_AND_DYNAMIC_CONTRACT_DROPIN.sh
```

This local-only drop-in:

1. verifies the uploaded job-18658301 review package and all nested hashes;
2. independently freezes one-seed safety, power, rate, runtime and sparsity
   evidence;
3. records an immutable execution overlay for stale bundle metadata;
4. identifies the current proportional-budget common-scale degeneracy;
5. updates project status, next step, roadmap and compute history;
6. freezes the first delayed/rate-limited dynamic experiment contract;
7. freezes the controller-ready full-topology export specification;
8. adds canonical exact-direction, negative-phase and full-inter-cell
   accounting helpers;
9. builds a compact review bundle;
10. commits and pushes all readable evidence to GitHub.

It performs no SSH, Slurm, Nibi or Narval action.
