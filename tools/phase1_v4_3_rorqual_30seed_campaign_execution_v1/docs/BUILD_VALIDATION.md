# Build validation

Required release gates:

- Python in-memory syntax;
- Bash syntax;
- focused tests including native authorization-guard acceptance;
- exact immutable R2 package/review bindings;
- synthetic 30-seed success-return packaging and audit;
- ZIP CRC and internal manifests;
- no cache or compiled artifacts;
- clean extracted-package rerun.
