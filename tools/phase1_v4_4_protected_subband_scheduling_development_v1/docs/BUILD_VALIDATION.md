# Build validation

Release requires: Python AST parsing, Bash syntax checks, 14 focused and
regression tests, exact immutable-diagnosis audits, source-interface binding to
the frozen campaign worker, complete/partial/emergency return simulations,
clean-repository source and evidence push simulations, stale non-force push
rejection, terminal-continuation simulation, ZIP CRC and internal-manifest
verification, an extracted-package rerun, and a forbidden cache/compiled-
artifact scan.

The real preserved-channel v4.4 replay is not executable in the build
environment and is the next gate.
