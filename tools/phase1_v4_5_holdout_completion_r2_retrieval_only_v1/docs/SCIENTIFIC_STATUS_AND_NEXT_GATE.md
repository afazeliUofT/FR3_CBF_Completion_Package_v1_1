# Scientific status

All five pass jobs completed with exit `0:0`; assembly completed with exit `0:0`. Seed 44052 has scientific exit 42 because it retains floor failures, but it is structurally valid. The merged fresh holdout is valid across all 30 seeds, passes EESS safety for all 30 seeds, and shows significant primary utility superiority. The universal floor is not supported; the correct claim is feasibility-conditioned.

The only failed operation was local retrieval (`scp` exit 255 after the MFA connection closed). No computation is to be repeated.

## Next gate

Retrieve and archive the existing remote return, then finalize the 13-page IEEE TWC manuscript and one compact practicality/limitations section. No extra seeds or controller variants are authorized.
