# WSL/Rorqual workflow

The wrapper runs local syntax/tests/audits, pushes source without force, submits a
CPU-only Rorqual array over the preserved eleven seeds, retrieves a compact return
on success or failure, and leaves the original WSL terminal open.
