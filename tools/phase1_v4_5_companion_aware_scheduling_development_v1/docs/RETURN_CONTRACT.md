# Return contract

Return the compact v4.5 ZIP, its basename-only SHA-256 sidecar, and
`LOCAL_WSL_ORCHESTRATOR.log`. Scientific exit 42 is a valid incomplete result and
must not trigger an automatic rerun.
