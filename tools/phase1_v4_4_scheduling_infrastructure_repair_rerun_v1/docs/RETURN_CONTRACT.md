# Return contract

Return exactly:

1. `FR3_RORQUAL_V4_4_SCHEDULING_INFRA_REPAIR_R1_<array-job-id>.zip`
2. its basename-only `.zip.sha256` sidecar
3. `LOCAL_WSL_ORCHESTRATOR.log`

Also paste the final printed values for worker/merge job IDs and states,
return hash and completeness, scientific status, hard-gate counts, scheduling
success/failure counts, floor/EESS totals, next repair decision, Git commits,
and terminal-continuation markers.
