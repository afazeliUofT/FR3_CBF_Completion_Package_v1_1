# Return contract

Return exactly:

- `FR3_RORQUAL_V4_5_HOLDOUT_COMPLETION_R1_<seed-job-id>.zip`
- its basename-only `.zip.sha256` sidecar
- `LOCAL_WSL_ORCHESTRATOR.log`

The final printed block should include the seed and merge Slurm states, holdout validity, 30-seed primary estimate and confidence interval, floor status, Git commits, next gate, and terminal-continuation markers.
