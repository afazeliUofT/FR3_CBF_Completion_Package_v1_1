Return exactly:

- `FR3_RORQUAL_V4_5_FRESH_HOLDOUT_<array-job-id>.zip`
- its basename-only `.zip.sha256` sidecar
- `LOCAL_WSL_ORCHESTRATOR.log`

The return is valid whether the floor is zero or feasibility-conditioned. A nonzero result is not an instruction to add seeds or tune another algorithm.
