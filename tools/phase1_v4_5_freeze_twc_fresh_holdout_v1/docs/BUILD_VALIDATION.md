# Build validation

The release is accepted only after Python and Bash syntax checks, focused tests, exact v4.5 evidence binding, deterministic job-package hash/CRC/manifest verification, native authorization-guard acceptance, synthetic success and scientific-nonzero return packaging, LaTeX compilation, normal and stale Git push simulations, failure-return packaging, terminal-continuation simulation, clean extracted-package rerun, and a compiled/cache-artifact scan.

The real Rorqual holdout is not executable in the build environment and is the next gate.
