# Return contract

Return exactly:

1. `FR3_RORQUAL_CANDIDATE_V4_3_H100_DEPLOYMENT_SMOKE_<job-id>.zip`, or the corresponding failure ZIP;
2. its basename-only `.zip.sha256` sidecar;
3. `LOCAL_WSL_ORCHESTRATOR.log`.

Also paste the final printed block containing the wrapper exit codes, Slurm job ID/account/state/ExitCode/MaxRSS, channel hashes, H100 environment and trace-reproduction gates, candidate hard gates, Git commits, campaign authorization, and next gate.
