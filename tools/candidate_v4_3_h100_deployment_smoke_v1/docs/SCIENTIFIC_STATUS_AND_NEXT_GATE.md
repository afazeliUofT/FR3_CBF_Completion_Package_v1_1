# Scientific status and next gate

## Source-supported result

The frozen v4.3 CPU return is a genuine pass under the unchanged floor and EESS contracts. All 104 originally affected intervals are repaired. The selected actions remain inside the strict post-mode sector-power budget and a bounded sparse action scope.

The minimum achieved/floor ratio is `1.0000000004430856`, so an independent deployment replay is required because the smallest floor margin is deliberately conservative but numerically small.

## Independent inference

The original fairness failure is not a policy infeasibility:

- the persistent Site-02 user is repaired primarily by same-sector fixed-RZF stream-power redistribution;
- the terminal Site-07 user is repaired by sparse local sector backoff;
- no fairness-policy revision or admission change is scientifically justified at this gate.

The average final PF change relative to the reviewed predictive controller is `-0.0123082`; the duration-weighted PF change is `+0.00407748`. Thus the repair closes the hard fairness failure with negligible utility cost on this excluded seed.

## Remaining assumptions

Action-scope locality is verified. The exact information exchanged to assemble the LP constraints, controller latency, RZF reweighting signalling, PA/EVM implications, and multi-seed generalization are not yet certified.

## Immediate gate

Run one H100-node integration smoke using the preserved seed-43999 channel. A pass permits freezing v4.3 and independently reviewing campaign readiness. It does not itself authorize the 30-seed campaign.
