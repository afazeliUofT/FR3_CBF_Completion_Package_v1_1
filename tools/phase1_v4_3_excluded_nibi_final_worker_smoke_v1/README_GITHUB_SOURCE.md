# GitHub source subset

This is the reviewable source and build-evidence subset for the excluded
candidate-v4.3 Nibi final campaign-worker smoke. The immutable locked job and
review ZIPs are already stored at their canonical repository paths and are not
duplicated here. Their paths and exact hashes are recorded in
`immutable_bindings/LOCKED_CAMPAIGN_CANONICAL_PATHS.json`.

This source permits only the excluded seed-43999 smoke. It does not authorize
seeds 44000--44029.
