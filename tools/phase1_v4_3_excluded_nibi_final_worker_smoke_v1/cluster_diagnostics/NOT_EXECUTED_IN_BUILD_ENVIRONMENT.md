The real Nibi H100 final-worker smoke was not executed while building this
package. It requires the user's Alliance account, Nibi Slurm allocation, and
MFA-backed SSH access. All locally executable gates are recorded under
`local_diagnostics/`.
