# Independent review of the immutable phase-1 Nibi job package

## Verdict

`PASS_FOR_NIBI_DEPLOYMENT_SMOKE_PREPARATION_NOT_FULL_CAMPAIGN_EXECUTION`

Reviewed objects:

- repository commit:
  `20d65eeb0fcc53f649a4f3f716fa2780406b4810`;
- package ID:
  `bd18734787de7396b9de4bf8c0b9ba4d191c6346a7cb83e969499622067a8129`;
- job-package ZIP SHA-256:
  `a81f1808f75119e64a0f7f631a54230f3e722efa8d17dcf032ee1296f2bb76be`;
- package-review ZIP SHA-256:
  `8620229b75767c97935b9fb65b8441cec8f677c6d56e452ba2fffd15b3a24b1d`.

The package passes source binding, manifest, candidate/review lineage, compute
DAG, result-schema, execution-lock, and exact local all-eight-method smoke
checks. The reactive-myopic comparator uses current coupling only and receives
the same sector-selective emergency fallback as the predictive controller.

## Scope of this PASS

The PASS permits preparation of a **noncampaign Nibi deployment smoke**. It
does not authorize the 30-seed array, does not authorize merge execution, and
does not create a paper result.

The noncampaign smoke must use a seed outside 44000--44029 and label all output
`EXCLUDED_FROM_PHASE1_CONFIRMATORY_ANALYSIS`. Its authorization token must be
smoke-scoped and bound to the package ID, job-package ZIP hash, reviewed commit,
candidate hash, and a frozen Nibi environment fingerprint.

## Package strengths

1. One cellular channel/topology realization is generated per campaign seed
   and reused across all five passes and eight methods.
2. The primary final moving-average sum-log utility and mandatory
   duration-weighted secondary utility are frozen.
3. The five pass records, generic 64-RF architecture, 65 dB null cap, 3 dB
   declared uplift, safety/fairness metrics, and seed-cluster bootstrap are
   bound by immutable manifests.
4. Predictive, safe-static, reactive-myopic, unshielded-myopic, virtual-queue,
   uniform-backoff, hard-null/mute, and noncausal-common-scale methods are all
   represented.
5. Submission, campaign, and merge entry points are locked.

## Local scientific smoke

For slot 0, the predictive and reactive-myopic methods with the same fallback
are both hard-safe and eligible-floor-safe. Predictive minus safe-static final
moving-PF utility is positive (`+0.209067`). Unshielded myopic and virtual queue
violate the long-term declared bound for 65 and 158 seconds, respectively.

This is useful package validation, not campaign evidence.

## Mandatory conditions before full phase-1 execution

- independently review a noncampaign Nibi deployment smoke;
- freeze an exact Nibi environment record;
- review the final WSL-to-Nibi orchestrator and full-execution token;
- bind the token to the package, environment, allowed seed list, stage, and
  expiry;
- create a master seed-result hash index and stronger external trace
  consistency checks;
- do not use the current per-method runtime field for comparative claims,
  because some entries measure only incremental fallback work;
- preserve the deterministic-engineering, noncalibration, and noncompliance
  boundaries.

Campaign execution remains unauthorized.
