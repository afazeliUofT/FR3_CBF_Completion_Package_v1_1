# Physical impairment sensitivity audit

This stage preserves the ideal corrected dual-criterion result but shows that its near-threshold 60--70 dB spatial-mode commands are not practical claims. Null-depth caps, differential array/steering errors, and phase quantization are deterministic engineering sensitivities only. Measured or source-referenced calibration data and a null-floor-aware fallback are required before a multi-seed campaign.
