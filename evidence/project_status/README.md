# Audited project-status evidence

These files are concise decision and validation records copied from the local
evidence workspace.

They establish that:

- the operator-independent publication route is frozen;
- no operator-specific fixed-service short-term cap is claimed;
- +19 dB was rejected as a uniform always-on fixed-service cap;
- the P.530 model-range warning was reviewed and retained as nonbinding;
- ITU-R P.452-18 reference implementation v18.0 passed its reference tests
  under MATLAB R2026a;
- the project-specific P.452 profile/MATLAB/export pilot passed.

Large inputs, full logs, raw trajectories, rasters, TAFL extracts, TLE files,
and complete result directories remain outside normal Git tracking. They must
be placed in a versioned release/research archive before paper submission.

The SHA-256 evidence manifests may reference local archived files that are not
present in the source-only Git repository.
