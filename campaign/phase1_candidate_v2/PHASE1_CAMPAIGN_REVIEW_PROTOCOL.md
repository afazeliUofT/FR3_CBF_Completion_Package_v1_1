# Phase-1 campaign candidate review protocol

The candidate is intentionally non-executable. Independent review must verify:

1. the 64T64R subarray grouping, phase convention, power normalization, and
   effective steering transformation;
2. full-load and load-transition local-RZF ranks and condition numbers;
3. exact incumbent safety and eligible-user floor accounting;
4. the sector-mute endpoint and its non-hidden incidence;
5. paired method definitions and identical information/delay/slew conditions;
6. seed list, protected-pass provenance, metrics, and clustered statistics;
7. full separation between declared engineering scenarios and measured
   calibration;
8. all five immutable protected-pass records.

Execution remains blocked while any pass slot is marked
`MISSING_IMMUTABLE_PASS_RECORD` or while the independent verdict is not PASS.


## Five-pass completion status

All five immutable protected-pass records are now present. Execution remains blocked pending independent source review and an explicit authorization token.
