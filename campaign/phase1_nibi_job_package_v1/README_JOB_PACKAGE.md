# Immutable phase-1 Nibi job-array package

This is a review candidate, not an execution package.

- Thirty H100 tasks generate one cellular channel each.
- Each task reuses its channel across five fixed pass records and eight methods.
- The canonical source is `PHASE1_CAMPAIGN_CONTRACT_V3.json`.
- The reactive-myopic comparator has no future coupling information and receives
  the same sector-selective emergency fallback.
- The primary endpoint is final moving-average sum-log utility.
- Submission and merge launchers are locked and return exit code 64.

Execution requires a separately reviewed authorization token matching
`PACKAGE_ID.txt`; no such token is included.
