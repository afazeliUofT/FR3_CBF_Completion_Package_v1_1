# Candidate-v4.3 immutable phase-1 Nibi job package

This is an execution-locked review candidate, not authorization to run.

- 30 seed clusters, five fixed passes, nine methods, 1,350 cells.
- Candidate v4.3 is the proposed method; safe static is the primary comparator.
- One H100-generated channel is reused across all passes and methods for each seed.
- Candidate hard gates are floor, long/short EESS, strict local action scope, strict post-mode power, no q0-only action, no unresolved action, and no network-wide shutdown.
- Scientific failures are packaged and returned with exit code 42.
- Merge is designed for an `afterany` dependency.
- Information-exchange locality is not yet certified.
- A future token must bind the exact full package manifest, candidate source, freeze commit, stage, seed list, and expiry.
- No authorization token is included; all launchers are locked.
