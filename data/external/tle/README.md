# TLE inputs for E3

Place the exact archived two-line element set for the selected satellite/pass here, together with its retrieval timestamp and source in `data/real/DATA_PROVENANCE.md`.

Use `scripts/06_generate_tle_track.py` only after installing the optional orbit dependency. Validate the generated topocentric track against an independent trusted tool before using it as paper evidence.
